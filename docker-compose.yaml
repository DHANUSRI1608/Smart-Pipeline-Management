version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: esp32
      POSTGRES_USER: esp32
      POSTGRES_PASSWORD: esp32
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
